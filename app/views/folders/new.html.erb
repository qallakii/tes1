<h1>New Folder</h1>

<%= form_with(model: @folder, local: true) do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit "Create Folder", class: "button" %>
<% end %>

<%= link_to "Back", folders_path, class: "button" %>
