<h2>Your Folders</h2>

<%= link_to "Create New Folder", new_folder_path, class: "bg-green-500 hover:bg-green-700 text-white py-2 px-4 rounded mb-4 inline-block" %>

<ul>
  <% @folders.each do |folder| %>
    <li style="margin-bottom: 10px;">
      <%= link_to folder.name, folder_path(folder), class: "text-blue-600 hover:underline" %>
      (<%= folder.cvs.count %> CVs)
    </li>
  <% end %>
</ul>

<% if @folders.empty? %>
  <p>You have no folders yet.</p>
<% end %>
